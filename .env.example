# Dev: optional override (default is http://127.0.0.1:3000)
# VITE_SOCKETHR_API_BASE=http://127.0.0.1:3000

# Production build: use .env.production (see .env.production.example) or:
# VITE_SOCKETHR_API_BASE=https://api.sockethr.com npm run build
#
# Public deploy + tunnel guide: docs/PUBLIC_TUNNEL.md
