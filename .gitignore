node_modules/
dist/
.DS_Store
data/
# Allow marketing blog data under src (repo root `data/` is still ignored for job uploads)
!src/data/
!src/data/**
.env.production
server/.env

# Local cloudflared config if copied into repo by mistake (real file lives in ~/.cloudflared/)
deploy/cloudflared/config.yml
