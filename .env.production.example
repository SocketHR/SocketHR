# Next.js production env example
#
# If unset, the app still uses public/runtime-config.json (apiBase).
# Set this only when you want build/runtime override.

NEXT_PUBLIC_SOCKETHR_API_BASE=https://api.sockethr.com
